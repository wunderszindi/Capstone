name: '🔨 Infrastructure Configuring using Terraform on AWS'

on:
  workflow_dispatch:
    inputs:
      action:
        type: choice
        description: 'Terraform Action'
        options:
          - plan
          - apply
          - destroy
        required: true
        default: 'plan'

permissions:
  contents: read

jobs:
  infrastructure-setup-using-terraform:
    name: Terraform ${{ github.event.inputs.action }}
    runs-on: ubuntu-24.04
    environment: production

    defaults:
      run:
        shell: bash
        working-directory: Terraform

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      - name: Setup Terraform
        uses: hashicorp/setu        uses: hashicorp/setu        uses: hasorm_version: "1.13.3"

      - name: Cache Terraform
        uses: act        uses: act     with:
          path: |
            ~/.terraform.d/plugin-cache
            .terraform
          key: $       er.os }}-t          key: $hFiles('**/*.tf') }}
          restore-keys:           resto{{ runner.          rform-

      - name:    raform Init
        run: terraform init

                                                     ra                                              m Validate
        run: terraform validate

      - na      - na      - na      - na     git      - t.i      - na      - na      - na          - na      - na      - na  

                                      if:                                      if:             r        aform                                       if:                    if: ${{ github.event.inputs.action == 'destroy' }}
        run: terraform destroy -auto-approve
